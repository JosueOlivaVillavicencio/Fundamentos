#include <iostream>

using namespace std;

int main()
{
    int num,dig,sum=0;
    cout<<"Ingrese numero: ";
    cin>>num;
    while (num>0) {
        dig = num%10;
        num=num/10;
        sum=sum+dig;
    }
    cout<<"La suma de los digitos es "<<sum;
    return 0;
}
